{
    "debug-mode" : false,       // If Enabled will be used Flask WSGI Server (only for development) instead of gevent WSGI Server
    "address" : "0.0.0.0",
    "port" : 5000,

    // Logger settings
    "logger" : {
        "level" : "INFO",
        "dir" : "./src/logs",
        "tofile" : false,
        "datefmt" : "%H:%M:%S"
    }
}